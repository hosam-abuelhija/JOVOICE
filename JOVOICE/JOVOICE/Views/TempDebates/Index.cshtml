@model IEnumerable<JOVOICE.Models.TempDebate>

@{
    ViewBag.Title = "Debates Applications";
    Layout = "~/Views/Shared/_LayoutPageAdmin.cshtml";
}

<h2>Debates Applications</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.listname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.areaelection)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.time)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.listname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.areaelection)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        
        <td>
            <button type="submit" name="approvedDebateId" value="@item.id" class="btn btn-primary"> Accept </button>
            @Html.ActionLink("Details", "Details", new { id = item.id }, new { @class = "btn btn-secondary" })
            @Html.ActionLink("Delete", "Delete", new { id = item.id }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
